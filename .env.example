# Use preview mode only for demonstrations. No message is delivered.
CONTACT_FORM_MODE=preview

# Production delivery endpoint. It receives a JSON payload over HTTPS.
CONTACT_WEBHOOK_URL=

# Optional bearer token sent to the delivery endpoint.
CONTACT_WEBHOOK_TOKEN=

# Long random value used to hash rate-limit identifiers.
CONTACT_RATE_LIMIT_SECRET=
